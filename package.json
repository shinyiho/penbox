{
  "name": "penbox",
  "version": "0.0.1",
  "description": "Canvas project.",
  "scripts": {
    "start": "parcel src/index.html --open",
		"predeploy": "rm -rf dist && parcel build src/index.html --public-url https://shinyiho.github.io/penbox",
		"build": "rm -rf dist && parcel build src/index.html",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "express": "^4.17.1",
    "parcel-bundler": "^1.12.4",
    "file-saver": "^2.0.5",
    "@babel/core": "^7.12.3"
  },
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
